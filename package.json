{
  "name": "ncf-test",
  "description": "NCF Test is New CSS Features Test by checking Syntax Parsing.",
  "version": "0.0.1",
  "devDependencies": {
    "grunt": "*",
    "grunt-cli": "*",
    "grunt-contrib-jshint": "*"
  },
  "scripts": {
    "test": "node_modules/.bin/grunt travis"
  }
}
